<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/image_head_height"
    android:background="#EE333333"
    android:focusable="true"
    android:focusableInTouchMode="true" >

    <View
        android:id="@+id/sp2"
        android:layout_width="1dp"
        android:layout_height="match_parent"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="10dp"
        android:layout_toLeftOf="@+id/preview"
        android:background="#44000000" />

    <TextView
        android:id="@+id/text_folder"
        android:layout_width="wrap_content"
        android:minWidth="@dimen/layout_folder_name_width"
        android:gravity="center"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="10dp"
        android:background="@drawable/image_more_sel"
        android:textColor="@drawable/image_text_btn"
        android:textSize="15sp" />

    <TextView
        android:id="@+id/preview"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginBottom="8dp"
        android:layout_marginRight="10dp"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:enabled="false"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:text="@string/preview"
        android:textColor="@drawable/image_text_btn"
        android:textSize="15sp" />

</RelativeLayout>